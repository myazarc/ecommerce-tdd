import request from "supertest";
import app from "../src/app";

describe("UserServices /user", () => {
  it("POST /register => created user", () => {});

  it("POST /login => created token", () => {});

  it("GET /confirmation/:usermailtoken => approved user mail", () => {});
});
